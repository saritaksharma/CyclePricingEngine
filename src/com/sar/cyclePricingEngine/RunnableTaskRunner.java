package com.sar.cyclePricingEngine;

import java.time.LocalDate;

public class RunnableTaskRunner {

	public static void main(String[] args) {

		/*
		 * LocalDate date = LocalDate.now().minusYears(4);
		 * 
		 * Cycle cycle = new Cycle(Frame.IRON_FRAME, new Handlebar("Spike",
		 * Gear.FOUR_GREAR), "seating", new Wheel("spokes", "rim", Tyre.TUBE_TYPE),
		 * Chain.FOUR_CHAINING, date); Thread thread = new Thread(cycle);
		 * thread.start();
		 */
	}

}
